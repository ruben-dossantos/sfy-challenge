import { Layout, Typography } from "antd";

const Header = () => {
  return (
    <Layout.Header>
      <Typography style={{ color: "#fff", fontSize: 20, lineHeight: "64px" }}>
        Sfy
      </Typography>
    </Layout.Header>
  );
};

export default Header;
